import React from "react";
import { Page } from "@components";
import { Maze } from "@pages";

const Activities = () => {
   return (
      <Page>
         <div>Activitites</div>
         <Maze></Maze>
      </Page>
   );
};

export default Activities;
